
export PROJECT=dev_qemu_glibc_labs
export PROJECT_BUILD=`pwd`/build
export PROJECT_ROOT=${PROJECT_BUILD}/${PROJECT}

mkdir -p ${PROJECT_ROOT}/{bootloader/data,build-tools/data,download,\
doc,images,kernel/data,project,rootfs,sysapps/data,tmp,tools,bin}

# Qemu
cp `pwd`/scripts/qemu_glibc/start_qemu.sh ${PROJECT_ROOT}/bin
cp `pwd`/scripts/qemu_glibc/uboot-test.sh ${PROJECT_ROOT}/bin
cp `pwd`/scripts/qemu_glibc/kernel-test.sh ${PROJECT_ROOT}/bin
cp `pwd`/scripts/qemu_glibc/build_pflash.sh ${PROJECT_ROOT}/bin

# Tools
cp `pwd`/scripts/common/list-libs.sh ${PROJECT_ROOT}/bin

# Toolchain
cp `pwd`/scripts/common/crosstool-ng.sh ${PROJECT_ROOT}/bin
cp `pwd`/scripts/common/setenv.sh ${PROJECT_ROOT}/bin
cp `pwd`/scripts/qemu_glibc/verify-toolchain.sh ${PROJECT_ROOT}/bin
cp `pwd`/data/qemu_glibc/crosstool-ng.config ${PROJECT_ROOT}/build-tools/data
cp `pwd`/data/common/hello.c ${PROJECT_ROOT}/build-tools/data
cp `pwd`/data/common/Makefile ${PROJECT_ROOT}/build-tools/data

# U-Boot
cp `pwd`/scripts/qemu_glibc/u-boot.sh ${PROJECT_ROOT}/bin
cp `pwd`/data/qemu_glibc/u-boot.config ${PROJECT_ROOT}/bootloader/data

# Linux kernel
cp `pwd`/scripts/common/kernel.sh ${PROJECT_ROOT}/bin
cp `pwd`/data/qemu_glibc/linux.config ${PROJECT_ROOT}/kernel/data

# Busybox
cp `pwd`/scripts/common/busybox.sh ${PROJECT_ROOT}/bin
cp `pwd`/data/common/busybox.config ${PROJECT_ROOT}/sysapps/data

cat > ./build/dev_qemu_glibc << "EOF"
export PROJECT=dev_qemu_glibc_labs
export PROJECT_ROOT=`pwd`/${PROJECT}
export ARCH=arm
export TARGET=arm-unknown-linux-gnueabihf
export CROSS_COMPILE=${TARGET}-
export PREFIX=${PROJECT_ROOT}/tools
export TARGET_PREFIX=${PREFIX}/${TARGET}
export PATH=${TARGET_PREFIX}/bin:${PROJECT_ROOT}/bin:${PATH}
export KERNEL_IMAGE=zImage
export KERNEL_PATH=${PROJECT_ROOT}/kernel/linux-stable/arch/${ARCH}/boot
export DTB_IMAGE=vexpress-v2p-ca9.dtb
export DTB_PATH=${KERNEL_PATH}/dts
export UBOOT_IMG=u-boot
export UBOOT_PATH=${PROJECT_ROOT}/bootloader/u-boot
cd ${PROJECT_ROOT}
EOF
